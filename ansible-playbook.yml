---
- hosts: localhost
  become: true
  gather_facts: false
  connection: local
  roles:
      - ansible-container
      - apt-utils
      # - dumb-init
      - doc2vec
      - python2
      - python3
      - jupyter
      - gensim
      - mecab
      - fasttext
      - jumanpp
      - juman
      - knp
      - pyknp
      - nltk
      - spacy

